14. Longest Common Prefix
Write a function to find the longest common prefix string amongst an array of strings.

If there is no common prefix, return an empty string "".

Example 1:
Input: ["flower","flow","flight"]
Output: "fl"

Example 2:
Input: ["dog","racecar","car"]
Output: ""
Explanation: There is no common prefix among the input strings.

Note:
All given inputs are in lowercase letters a-z.

class Solution {
    public String longestCommonPrefix(String[] strs) {
        String finalSubstring = "";
        if(strs.length == 0)
        {
            return finalSubstring;
        }
        
        String startString = strs[0];
        if(strs.length == 1)
        {
            return startString;
        }
        for(int j=0; j<startString.length(); j++)
        {
            char c = startString.charAt(j);
            for(int i=1; i<strs.length; i++)
            {
                String curString = strs[i];
                if(j >= curString.length())
                {
                    return finalSubstring;   
                }
                
                char curCompChar = curString.charAt(j);
                
                if(curCompChar != c)
                {
                    return finalSubstring;
                }
                
            }
            finalSubstring += c;
        }
        return finalSubstring;
    }
}